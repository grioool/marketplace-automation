export enum NavigationPath {

    LOGIN = "/login",
    REGISTRATION = "/registration",
    MAIN = "/main",
    SUPPLIES = "/supplies",
    PURCHASES = "/purchases",
    REPORTS = "/reports",
    USERS = "/users",
    PROFILE = "/profile",
    INFORMATION = "/information",
    SALES = "/sales",
    ORDERS = "/orders",
}
